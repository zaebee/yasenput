YPApp.module("Event", function(Event, YPApp, Backbone, Marionette, $, _){
    Event.Controller = Marionette.Controller.extend({
        initialize: function (options) {

        }}
    )
	YPApp.addInitializer(function(options){

	});
});